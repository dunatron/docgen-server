# import Document, Section from "./generated/prisma.graphql"

type Query {
  info: String!
  documentFeed: [Document!]!
  singleDocument: [Document]
  allUsers: [User!]!
}

type Mutation {
  postDocument(name: String): Document!
  postSection(name: String, belongsTo: ID): Section!
  updateDocument(id: ID!, name: String): Document!
  signup(email: String!, password: String!, name: String!): AuthPayload
  login(email: String!, password: String!): AuthPayload
}

# type Document {
#   id: ID!
#   name: String
# }

type AuthPayload {
  token: String
  user: User
}

type User {
  id: ID!
  name: String!
  email: String!
  documents: [Document!]!
}
